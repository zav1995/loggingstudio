import { Stack, Text, Title } from '@mantine/core';

export function Parsers() {
  return (
    <Stack>
      <Title order={2}>Parsers</Title>
      <Text c="dimmed">Parsers view — placeholder.</Text>
    </Stack>
  );
}
