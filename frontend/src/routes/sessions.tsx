import { Stack, Text, Title } from '@mantine/core';

export function Sessions() {
  return (
    <Stack>
      <Title order={2}>Sessions</Title>
      <Text c="dimmed">Sessions view — placeholder.</Text>
    </Stack>
  );
}
