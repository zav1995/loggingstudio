import { Stack, Text, Title } from '@mantine/core';

export function Rejected() {
  return (
    <Stack>
      <Title order={2}>Rejected ingestions</Title>
      <Text c="dimmed">Rejected ingestions view — placeholder.</Text>
    </Stack>
  );
}
