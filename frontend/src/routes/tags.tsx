import { Stack, Text, Title } from '@mantine/core';

export function Tags() {
  return (
    <Stack>
      <Title order={2}>Tags</Title>
      <Text c="dimmed">Tags view — placeholder.</Text>
    </Stack>
  );
}
